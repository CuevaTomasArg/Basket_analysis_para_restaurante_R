# bloc de notas para hacer un check list de lo que voy a hacer especificamente.

## Explorar y describir el dataset
-> Tengo que usar "./scripts/New_Manipulation_data.R" y poder describirlo
-> Tengo que usar la funcion de "./scripts/descript_dataframe.R"

IMPORTANTE: Hay tres datos que tengo que eliminar, lo cuales tienen los años
mal puestos y ademas le faltan datos, eliminar esos registros.

NOTA: Tengo que filtrar esos datos teniendo en cuenta que los años figuran despues
de 2023.

## ¿Que debe tener el informe?
Debe detallar el comportamiento de las variables y las relaciones entre las mismas. Incluyendo graficos. Teniendo en cuenta además, las siguientes rubricas:

### Funciones
Definir una funcion para limpiar datos NA/-/?, Si los hay informar y cuantos se eliminan

NOTA:Esto esta en el script de "descript_dataframe.R" el cual tengo que  agregarle:

-> Sacar esos datos que me faltan de los años.

### Tidyverse
Se incluye el pipe, junto con mutate/select/transmutate/relocate/rename/filter.
NOTA: Esto se realiza indefectiblemente a la hora de limpiar el dataset

### Estructuras de control
Existe por lo menos una estructura de control if-else

NOTA: Esto ya va a existir dentro de la limpieza de datos, sin embargo se puede llegar a agregar algun que otra condicion if.

### Iteración
En todas las transformaciones de la data se requere realizar una o más iteraciones en el proyecto, ya sea en base R( for loop) o con purr(con ma o alguna variante)

NOTA: El purr se vio en la ultima clase con las etiquetas
IDEA: Puedo usar lo visto en la ultima clase para crear un arbol de desicion en donde puedo meter todos los datos de nacionalidad, equipo y demás para saber si tiene probabilidad de estar en el ranking 200 del propio dataset, CREAR UN ARBOL DE DESICIÓN.
IDEA:Con la Iteración puede ser posible hacer una mezcla entre nadadores, entre los nadadores hombres y mujeres, poder hacer un ranking general en donde en vez de que sean 200, sean el total del ranking 400 incluyendo a hombres y mujeres, unicamente para diversión.

### Trasformaciones
El dataset tiene todas las debidas transformaciones. Esto ya se hace desde el principio junto con la descripcion del dataset, de igual forma se detallaran las normas de realización de acuerdo al final de estas notas.

### Stortelling
El informe esta bien organizado con secciones y en cada una de ellas hayu testo para explicar en datalle lo que se estsa resaliando.
Cada cosa tiene que estar explicada, en si, cada linea de codigo la voy a tener que explicar


# NORMATIVA DE REALIZACIÓN

1ro -> Descripción del dataset

2do-> Primera transformación del dataset

3ro -> Cantidad de nadadores por Pais "./script/swimmers_per_team.R", hay un grafico, sin embargo esto tiene que estar en una tabla interactiva como en la pre-entrega

4to -> Diferencias entre homres "./script/difference_M&F.R"

5to -> Edad promedio de los nadadores: "age_per_swimmer.R"

6to -> Crear un arbol de regresión en donde pueda predesir, si es de un equipo, entonces es probable que esté en el ranking.

7mo -> Unir el ranking de hombres y mujeres, cambiar el ranking de 200 por ranking de 400 por cada carrera.